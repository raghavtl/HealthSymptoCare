{
  "name": "nithya",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "admin": "cd admin && npm run dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "dev:all": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run admin\"",
    "init-db": "cd backend && node config/init-db.js",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../admin && npm install",
    "build": "cd frontend && npm run build",
    "build:admin": "cd admin && npm run build",
    "build:all": "npm run build && npm run build:admin",
    "deploy": "node deploy.js",
    "preview": "cd frontend && npm run preview",
    "heroku-postbuild": "npm run install-all && npm run build:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7",
    "http-proxy-middleware": "^2.0.6"
  }
}
